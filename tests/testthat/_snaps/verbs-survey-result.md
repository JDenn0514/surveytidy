# rename_with.survey_result errors for all invalid .fn outputs

    Code
      dplyr::rename_with(result_means, fn)
    Condition
      Error in `dplyr::rename_with()`:
      x `.fn` must return a character vector the same length as its input with no `NA` or duplicate names.
      i Got class <integer> of length 4.

---

    Code
      dplyr::rename_with(result_means, fn)
    Condition
      Error in `dplyr::rename_with()`:
      x `.fn` must return a character vector the same length as its input with no `NA` or duplicate names.
      i Got class <character> of length 1.

---

    Code
      dplyr::rename_with(result_means, fn)
    Condition
      Error in `dplyr::rename_with()`:
      x `.fn` must return a character vector the same length as its input with no `NA` or duplicate names.
      i Got class <character> of length 4.

---

    Code
      dplyr::rename_with(result_means, fn)
    Condition
      Error in `dplyr::rename_with()`:
      x `.fn` must return a character vector the same length as its input with no `NA` or duplicate names.
      i Got class <character> of length 4.

