% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reexports.R, R/select.R
\name{select}
\alias{select}
\alias{select.survey_base}
\alias{select.survey_result}
\alias{select.survey_collection}
\title{Keep or drop columns using their names and types}
\usage{
select(.data, ...)

\method{select}{survey_base}(.data, ...)

\method{select}{survey_result}(.data, ...)

\method{select}{survey_collection}(.data, ..., .if_missing_var = NULL)
}
\arguments{
\item{.data}{A \code{\link[surveycore:survey_base]{survey_base}} object, or a
\code{survey_result} object returned by a surveycore estimation function.}

\item{...}{<\code{\link[tidyselect:language]{tidy-select}}> One or more unquoted
column names or tidy-select expressions.}

\item{.if_missing_var}{Per-call override of \code{collection@if_missing_var}.
One of \code{"error"} or \code{"skip"}, or \code{NULL} (the default) to inherit the
collection's stored value. See
\code{\link[surveycore:set_collection_if_missing_var]{surveycore::set_collection_if_missing_var()}}.}
}
\value{
An object of the same type as \code{.data} with the following properties:
\itemize{
\item Rows are not modified.
\item Non-selected, non-design columns are permanently removed.
\item Design variables are always retained.
\item Survey design attributes are preserved.
}
}
\description{
\code{select()} keeps the named columns and drops all others, using the
\link[tidyselect:language]{tidyselect mini-language} to describe column sets.
Design variables (weights, strata, PSU, FPC, replicate weights) are
\strong{always retained} even when not explicitly selected — they are required
for variance estimation. After \code{select()}, \code{print()} shows only the columns
you selected; design variables remain in the object but are hidden from
display.

\code{select()} is irreversible: dropped columns are permanently removed from
the survey object and cannot be recovered within the same pipeline.
}
\details{
\subsection{Design variable preservation}{

Regardless of what you select, the following are always kept in the
survey object: weights, strata, PSUs, FPC columns, replicate weights,
and the domain column (if set by \code{\link[=filter]{filter()}}). They are hidden from
\code{print()} output but remain available for variance estimation.
}

\subsection{Metadata}{

Variable labels, value labels, and other metadata for dropped columns
are removed. Metadata for retained columns is preserved.
}
}
\section{Survey collections}{

When applied to a \code{survey_collection}, \code{select()} is dispatched to each
member independently. Each member resolves its own tidyselect expression
against its own \verb{@data}, so members may end up with different visible
columns when the selection is partial (e.g., \code{any_of()} against a
heterogeneous collection). Per-member design variables are always retained.

Before dispatching, \code{select.survey_collection} resolves the selection
against the first member's \verb{@data} and raises
\code{surveytidy_error_collection_select_group_removed} if any column in
\code{coll@groups} would be removed. Group columns must remain in every member;
silently dropping them would violate the surveycore class validator (G1b).
Use \code{\link[=ungroup]{ungroup()}} first if you intend to remove a group column.

\code{relocate.survey_collection} is \strong{not} subject to this pre-flight —
\code{dplyr::relocate} only reorders columns and never drops them.
}

\examples{
library(surveytidy)
library(surveycore)

# create a survey design from the pew_npors_2025 example dataset
d <- as_survey(pew_npors_2025, weights = weight, strata = stratum)

# select by name
select(d, gender, agecat)

# select by name pattern
select(d, tidyselect::starts_with("smuse_"))

# select by type
select(d, tidyselect::where(is.numeric))

# drop columns with !
select(d, !tidyselect::starts_with("smuse_"))

}
\seealso{
\code{\link[=relocate]{relocate()}} to reorder columns, \code{\link[=rename]{rename()}} to rename them,
\code{\link[=mutate]{mutate()}} to add new ones

Other selecting: 
\code{\link{glimpse}},
\code{\link{pull}},
\code{\link{relocate}}
}
\concept{selecting}
