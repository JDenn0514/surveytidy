% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{subset.survey_base}
\alias{subset.survey_base}
\title{Physically remove rows from a survey design object}
\usage{
\method{subset}{survey_base}(x, condition, ...)
}
\arguments{
\item{x}{A \code{\link[surveycore:survey_base]{survey_base}} object.}

\item{condition}{A logical expression evaluated against the survey data.
Rows where \code{condition} is \code{FALSE} or \code{NA} are removed.}

\item{...}{Ignored. Included for compatibility with the base \code{\link[=subset]{subset()}}
generic.}
}
\value{
An object of the same type as \code{x} with only matching rows retained. Always
issues \code{surveycore_warning_physical_subset}.
}
\description{
\code{subset()} physically removes rows from a
\code{\link[surveycore:survey_base]{survey_base}} object where \code{condition} evaluates
to \code{FALSE}. \strong{This changes the survey design.} Unless the design was
explicitly built for the subset population, variance estimates will be
incorrect.

For subpopulation analyses, use \code{\link[=filter]{filter()}} instead. \code{filter()} marks rows
as in or out of the domain without removing them, leaving the full design
intact for variance estimation.

\code{subset()} always emits a \code{surveycore_warning_physical_subset} warning as a
reminder of the statistical implications.
}
\examples{
library(surveytidy)
library(surveycore)
d <- as_survey(pew_npors_2025, weights = weight, strata = stratum)

# Physical row removal — always issues a warning
subset(d, agecat >= 3)

}
\seealso{
\code{\link[=filter]{filter()}} for domain-aware row marking (preferred for
subpopulation analyses)
}
