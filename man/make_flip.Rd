% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{make_flip}
\alias{make_flip}
\title{Flip the semantic valence of a variable}
\usage{
make_flip(x, label, .description = NULL)
}
\arguments{
\item{x}{A numeric vector. \code{typeof(x)} must be \code{"double"} or \code{"integer"}.}

\item{label}{\code{character(1)}. \strong{Required.} New variable label describing the
flipped semantic meaning.}

\item{.description}{\code{character(1)} or \code{NULL}. Transformation description.}
}
\value{
A numeric vector (same \code{typeof()} as \code{x}). Values are unchanged.
}
\description{
\code{make_flip()} reverses the label string associations of a numeric variable
without changing its values. This is used to flip the polarity of a survey
item for composite scoring - for example, converting "I like the color blue"
to "I dislike the color blue" without changing the underlying numeric codes.

Unlike \code{\link[=make_rev]{make_rev()}}, which changes numeric values and keeps label strings in
place, \code{make_flip()} keeps values unchanged and reverses which label strings
are attached to which values.

A new variable label is \strong{required} because flipping always changes the
semantic meaning of the variable.

When called inside \code{\link[=mutate]{mutate()}}, metadata is recorded in
\verb{@metadata@transformations[[col]]}.
}
\examples{
x <- c(1, 2, 3, 4)
attr(x, "labels") <- c("Strongly agree" = 1, "Agree" = 2,
                        "Disagree" = 3, "Strongly disagree" = 4)
make_flip(x, "I dislike the color blue")

}
\seealso{
Other transformation: 
\code{\link{make_binary}()},
\code{\link{make_dicho}()},
\code{\link{make_factor}()},
\code{\link{make_rev}()},
\code{\link{row_means}()},
\code{\link{row_sums}()}
}
\concept{transformation}
