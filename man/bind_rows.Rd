% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joins.R
\name{bind_rows}
\alias{bind_rows}
\title{Stack surveys with bind_rows (errors unconditionally)}
\usage{
bind_rows(x, ..., .id = NULL)
}
\arguments{
\item{x}{A \code{\link[surveycore:survey_base]{survey_base}} object (always errors),
or any object accepted by \code{\link[dplyr:bind_rows]{dplyr::bind_rows()}} (transparent delegation).}

\item{...}{Additional arguments.}

\item{.id}{Forwarded to \code{\link[dplyr:bind_rows]{dplyr::bind_rows()}}.}
}
\value{
Never returns when \code{x} is a survey object — always throws an error.
When \code{x} is not a survey object, returns the result of \code{\link[dplyr:bind_rows]{dplyr::bind_rows()}}.
}
\description{
\code{bind_rows()} errors unconditionally when the first argument is a survey
design object. Stacking two surveys changes the design — the combined object
requires a new design specification (e.g., a new survey-wave stratum).

When the first argument is not a survey object, this function delegates to
\code{\link[dplyr:bind_rows]{dplyr::bind_rows()}} transparently.
}
\details{
\strong{Known limitation:} If the survey object is passed as a \strong{non-first}
argument (e.g., \code{bind_rows(df, survey)}), this function delegates to
\code{dplyr::bind_rows(df, survey)} which will fail with a dplyr/vctrs error
rather than the survey-specific error. Always pass the survey object as the
first argument to ensure the correct error is triggered.
\subsection{Dispatch note}{

\code{dplyr::bind_rows()} uses \code{vctrs::vec_rbind()} internally for recent dplyr
versions and does not reliably dispatch via S3 on \code{x} for S7 objects.
surveytidy provides its own \code{bind_rows()} that intercepts survey objects
before delegating to dplyr (GAP-6 verified: S3 dispatch does not work;
standalone function approach used instead).
}
}
\examples{
# NOTE: do not load dplyr here — its bind_rows() would mask surveytidy's
# bind_rows() and bypass the survey-object check shown below.

# two raw data frames that together define a combined survey
df1 <- data.frame(wt = c(1, 1), y1 = c(1, 2))
df2 <- data.frame(wt = c(1, 1), y1 = c(3, 4))

# bind_rows() on plain data frames delegates to dplyr::bind_rows()
bind_rows(df1, df2)

# but bind_rows() on a survey object always errors — stacking two surveys
# would change the design, requiring a new design specification
d1 <- surveycore::as_survey(df1, weights = wt)

tryCatch(
  bind_rows(d1, df2),
  error = function(e) message(conditionMessage(e))
)

# the recommended workflow: extract raw data from each survey, bind, then
# re-specify the design on the combined data frame
combined <- bind_rows(
  surveycore::survey_data(d1),
  df2
)
surveycore::as_survey(combined, weights = wt)

}
\seealso{
Other joins: 
\code{\link{bind_cols}()},
\code{\link{inner_join}},
\code{\link{left_join}},
\code{\link{right_join}},
\code{\link{semi_join}}
}
\concept{joins}
