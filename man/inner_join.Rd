% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joins.R, R/reexports.R
\name{inner_join}
\alias{inner_join}
\title{Domain-aware inner join for survey designs}
\usage{
inner_join(
  x,
  y,
  by = NULL,
  copy = FALSE,
  suffix = c(".x", ".y"),
  ...,
  keep = NULL
)
}
\arguments{
\item{x}{A \code{\link[surveycore:survey_base]{survey_base}} object.}

\item{y}{A plain data frame. Must not be a survey object.}

\item{by}{A character vector of column names or a \code{\link[dplyr:join_by]{dplyr::join_by()}}
specification. \code{NULL} uses all common column names.}

\item{copy}{Forwarded to the underlying dplyr function.}

\item{suffix}{A character vector of length 2. Forwarded to the underlying
dplyr function for handling shared column names.}

\item{...}{Additional arguments forwarded to the underlying dplyr function.}

\item{keep}{Forwarded to the underlying dplyr function.}
}
\value{
A survey design object of the same type as \code{x}.
\itemize{
\item Domain-aware mode (\code{.domain_aware = TRUE}): row count unchanged;
\code{..surveycore_domain..} updated; new columns from \code{y} appended.
\item Physical mode (\code{.domain_aware = FALSE}): row count reduced to matched
rows; new columns from \code{y} appended.
}
}
\description{
\code{inner_join()} has two modes controlled by \code{.domain_aware} (default \code{TRUE}):

\strong{Domain-aware mode (\code{.domain_aware = TRUE}, default):} Unmatched rows are
marked \code{FALSE} in the domain column (exactly like \code{\link[=filter]{filter()}} or \code{\link[=semi_join]{semi_join()}}),
and \code{y}'s columns are added to all rows (with \code{NA} for unmatched rows). All
rows remain in \verb{@data}. Row count is unchanged. This is the survey-correct
default.

\strong{Physical mode (\code{.domain_aware = FALSE}):} Unmatched rows are physically
removed, exactly like base R \code{inner_join}. Emits
\code{surveycore_warning_physical_subset}. Errors for \code{survey_twophase} designs.
}
\details{
\subsection{Choosing a mode}{

The domain-aware default preserves variance estimation validity. The
\code{nrow()} behaviour (count stays the same) is consistent with \code{\link[=filter]{filter()}} and
\code{\link[=semi_join]{semi_join()}} precedents in surveytidy.

Physical mode (\code{.domain_aware = FALSE}) is appropriate only when you
explicitly want to reduce the design to a specific subpopulation. For
replicate designs (BRR, jackknife), physical row removal can corrupt
half-sample or pairing structure, producing numerically wrong variance
estimates. Domain-aware mode is recommended for replicate designs.
}

\subsection{Duplicate keys}{

Duplicate keys in \code{y} that would expand the row count are an error in both
modes. Deduplicate \code{y} with \code{dplyr::distinct()} before joining.
}

\subsection{The \code{.domain_aware} argument (survey-specific extension)}{

The surveytidy method adds one argument not present in the dplyr generic:
\code{.domain_aware = TRUE} (default) performs domain-aware joining; set
\code{.domain_aware = FALSE} for physical row removal (emits
\code{surveycore_warning_physical_subset}; errors for \code{survey_twophase}).
}
}
\examples{
library(dplyr)
library(surveycore)
df <- data.frame(
  psu = paste0("psu_", 1:5),
  strata = "s1",
  fpc = 100,
  wt = 1,
  y1 = 1:5
)
d <- as_survey(df, ids = psu, weights = wt, strata = strata, fpc = fpc,
               nest = TRUE)
lookup <- data.frame(y1 = 1:3, label = letters[1:3])
# Domain-aware: marks rows 4 and 5 as out-of-domain
inner_join(d, lookup, by = "y1")
# Physical: removes rows 4 and 5
inner_join(d, lookup, by = "y1", .domain_aware = FALSE)

}
\seealso{
Other joins: 
\code{\link{bind_cols}()},
\code{\link{bind_rows}()},
\code{\link{left_join}},
\code{\link{right_join}},
\code{\link{semi_join}}
}
\concept{joins}
