% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joins.R, R/reexports.R
\name{right_join}
\alias{right_join}
\alias{full_join}
\title{Unsupported joins for survey designs}
\usage{
right_join(
  x,
  y,
  by = NULL,
  copy = FALSE,
  suffix = c(".x", ".y"),
  ...,
  keep = NULL
)

full_join(
  x,
  y,
  by = NULL,
  copy = FALSE,
  suffix = c(".x", ".y"),
  ...,
  keep = NULL
)
}
\arguments{
\item{x}{A \code{\link[surveycore:survey_base]{survey_base}} object.}

\item{y}{A data frame or survey object.}

\item{by}{Ignored — the function always errors.}

\item{copy}{Ignored — the function always errors.}

\item{suffix}{Ignored — the function always errors.}

\item{...}{Additional arguments (ignored; the function always errors).}

\item{keep}{Ignored — the function always errors.}
}
\value{
Never returns — always throws an error.
}
\description{
\code{right_join()} and \code{full_join()} error unconditionally for survey design
objects because they can add rows from \code{y} that have no match in the survey.
Those new rows would have \code{NA} for all design variables (weights, strata,
PSU), producing an invalid design object.
}
\details{
Use \code{\link[=left_join]{left_join()}} to add lookup columns from \code{y}. Use \code{\link[=filter]{filter()}} or
\code{\link[=semi_join]{semi_join()}} to restrict the survey domain.
}
\examples{
\dontrun{
library(dplyr)
library(surveycore)
df <- data.frame(psu = "p1", strata = "s1", fpc = 10, wt = 1, y1 = 1)
d <- as_survey(df, ids = psu, weights = wt, strata = strata, fpc = fpc,
               nest = TRUE)
lookup <- data.frame(y1 = 1:2, label = c("a", "b"))
right_join(d, lookup, by = "y1")  # errors
full_join(d, lookup, by = "y1")   # errors
}

}
\seealso{
Other joins: 
\code{\link{bind_cols}()},
\code{\link{bind_rows}()},
\code{\link{inner_join}},
\code{\link{left_join}},
\code{\link{semi_join}}
}
\concept{joins}
