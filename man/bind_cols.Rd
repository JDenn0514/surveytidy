% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joins.R
\name{bind_cols}
\alias{bind_cols}
\title{Append columns to a survey design by position}
\usage{
bind_cols(x, ..., .name_repair = "unique")
}
\arguments{
\item{x}{A \code{\link[surveycore:survey_base]{survey_base}} object, or any object
accepted by \code{\link[dplyr:bind_cols]{dplyr::bind_cols()}}.}

\item{...}{One or more plain data frames or named lists. When \code{x} is a
survey object, none of the objects may be survey objects.}

\item{.name_repair}{Forwarded to \code{\link[dplyr:bind_cols]{dplyr::bind_cols()}}.}
}
\value{
When \code{x} is a survey object: a survey design object of the same type as \code{x}
with new columns appended to \verb{@data}. \code{visible_vars} is updated if it was
set. When \code{x} is not a survey object: the result of \code{\link[dplyr:bind_cols]{dplyr::bind_cols()}}.
}
\description{
\code{bind_cols()} appends columns from one or more plain data frames to a survey
design object, matching by row position. This is equivalent to an implicit
row-index \code{left_join()}. All rows are preserved; row count is unchanged.

When \code{x} is not a survey object, this function delegates to
\code{\link[dplyr:bind_cols]{dplyr::bind_cols()}} transparently.
}
\details{
\subsection{Design integrity}{

None of the objects in \code{...} may be a survey object. If any new column name
matches a design variable in \code{x}, that column is dropped with a warning.
All inputs in \code{...} must have exactly the same number of rows as \code{x}.
}

\subsection{Dispatch note}{

\code{dplyr::bind_cols()} uses \code{vctrs::vec_cbind()} internally and does not
dispatch via S3 on \code{x}. surveytidy provides its own \code{bind_cols()} that
intercepts survey objects before delegating to dplyr.
}
}
\examples{
library(surveytidy)
library(surveycore)
df <- data.frame(
  psu = paste0("psu_", 1:5),
  strata = "s1",
  fpc = 100,
  wt = 1,
  y1 = 1:5
)
d <- as_survey(df, ids = psu, weights = wt, strata = strata, fpc = fpc,
               nest = TRUE)
extra <- data.frame(label = letters[1:5])
bind_cols(d, extra)

}
\seealso{
Other joins: 
\code{\link{bind_rows}()},
\code{\link{inner_join}},
\code{\link{left_join}},
\code{\link{right_join}},
\code{\link{semi_join}}
}
\concept{joins}
