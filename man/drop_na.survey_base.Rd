% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07-tidyr.R
\name{drop_na.survey_base}
\alias{drop_na.survey_base}
\title{Remove rows containing missing values from a survey design object}
\usage{
\method{drop_na}{survey_base}(data, ...)
}
\arguments{
\item{data}{A survey design object.}

\item{...}{<\code{\link[tidyselect:language]{tidy-select}}> Columns to inspect for
\code{NA}. If empty, all columns are checked.}
}
\value{
The survey object with rows containing \code{NA} in the selected columns
removed.
}
\description{
Physically removes rows where the specified columns contain \code{NA}. If no
columns are specified, any \code{NA} in any column triggers removal. Always
issues \code{surveycore_warning_physical_subset}. Errors if all rows would be
removed.

Prefer \code{\link[dplyr:filter]{dplyr::filter()}} with \code{!is.na(col)} for subpopulation analyses — it keeps
all rows and gives correct variance estimates.
}
\examples{
library(tidyr)
df <- data.frame(y = c(rnorm(99), NA), wt = runif(100, 1, 5))
d  <- surveycore::as_survey(df, weights = wt)

# Remove rows with NA in y
d2 <- suppressWarnings(drop_na(d, y))
nrow(d2@data)  # 99

}
\seealso{
\code{\link[dplyr:filter]{dplyr::filter()}} for domain-aware row marking (preferred)

Other row operations: 
\code{\link{arrange.survey_base}()}
}
\concept{row operations}
