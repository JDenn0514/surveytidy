% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{make_binary}
\alias{make_binary}
\title{Convert a dichotomous variable to a numeric 0/1 indicator}
\usage{
make_binary(
  x,
  flip_values = FALSE,
  .exclude = NULL,
  .label = NULL,
  .description = NULL
)
}
\arguments{
\item{x}{Vector. Same types as \code{\link[=make_factor]{make_factor()}}. Must yield exactly 2 levels
(after \code{.exclude}) or error.}

\item{flip_values}{\code{logical(1)}. If \code{TRUE}, map the first level to \code{0L} and
the second to \code{1L}. Default maps first level to \code{1L}.}

\item{.exclude}{\code{character} or \code{NULL}. Passed directly to \code{\link[=make_dicho]{make_dicho()}}.
Level names to set to \code{NA} before encoding.}

\item{.label}{\code{character(1)} or \code{NULL}. Variable label override. Falls back
to \code{attr(x, "label")} then the column name.}

\item{.description}{\code{character(1)} or \code{NULL}. Transformation description.}
}
\value{
An integer vector with values \code{0L}, \code{1L}, or \code{NA_integer_}.
}
\description{
\code{make_binary()} converts a variable that can be collapsed to exactly two
levels (via \code{\link[=make_dicho]{make_dicho()}}) into an integer vector of 0s and 1s. By default,
the first level maps to \code{1L} and the second to \code{0L}. Use
\code{flip_values = TRUE} to reverse the mapping.

When called inside \code{\link[=mutate]{mutate()}}, metadata is recorded in
\verb{@metadata@transformations[[col]]}.
}
\examples{
# build a 2-level factor with one NA
x <- factor(
  c("Agree", "Disagree", "Agree", NA),
  levels = c("Agree", "Disagree")
)

# encode as a 0/1 integer indicator
make_binary(x)

}
\seealso{
Other transformation: 
\code{\link{make_dicho}()},
\code{\link{make_factor}()},
\code{\link{make_flip}()},
\code{\link{make_rev}()},
\code{\link{row_means}()},
\code{\link{row_sums}()}
}
\concept{transformation}
