% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rowstats.R
\name{row_means}
\alias{row_means}
\title{Compute row-wise means across selected columns}
\usage{
row_means(.cols, na.rm = FALSE, .label = NULL, .description = NULL)
}
\arguments{
\item{.cols}{<\code{\link[tidyselect:language]{tidy-select}}> Columns to average
across, evaluated via \code{\link[dplyr:pick]{dplyr::pick()}}. Typical values:
\code{c(a, b, c)}, \code{starts_with("y")}, \code{where(is.numeric)}. Must resolve to at
least one column, and all selected columns must be numeric.}

\item{na.rm}{\code{logical(1)}. If \code{TRUE}, \code{NA} values are excluded before
computing the mean. If all values in a row are \code{NA} and \code{na.rm = TRUE},
the result is \code{NaN} (matching base R \code{rowMeans()} behavior). Default
\code{FALSE}.}

\item{.label}{\code{character(1)} or \code{NULL}. Variable label stored in
\verb{@metadata@variable_labels[[col]]} after \code{mutate()}. If \code{NULL}, falls back
to the output column name from \code{\link[dplyr:context]{dplyr::cur_column()}}.}

\item{.description}{\code{character(1)} or \code{NULL}. Plain-language description of
the transformation stored in \verb{@metadata@transformations[[col]]$description}
after \code{mutate()}.}
}
\value{
A \code{double} vector of length equal to the number of rows in the
current data context.
}
\description{
\code{row_means()} computes the mean of each row across a tidyselect-selected set
of numeric columns. It is designed for use inside \code{\link[=mutate]{mutate()}} on survey design
objects. When called inside \code{mutate()}, the transformation is recorded in
\verb{@metadata@transformations[[col]]}.
}
\examples{
# create a dummy survey object
d <- surveycore::as_survey(
  data.frame(
    y1 = c(1, 2, 3),
    y2 = c(4, 5, 6),
    wt = c(1, 1, 1)
  ),
  weights = wt
)

# use a vector of columns to create the score
mutate(d, score = row_means(c(y1, y2)))

# use tidy-select for columns and add a label
d |>
  mutate(
    score = row_means(
      tidyselect::starts_with("y"),
      na.rm = TRUE,
      .label = "Score"
    )
  )

}
\seealso{
Other transformation: 
\code{\link{make_binary}()},
\code{\link{make_dicho}()},
\code{\link{make_factor}()},
\code{\link{make_flip}()},
\code{\link{make_rev}()},
\code{\link{row_sums}()}
}
\concept{transformation}
