% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joins.R, R/reexports.R
\name{semi_join}
\alias{semi_join}
\alias{anti_join}
\alias{semi_join.survey_collection}
\alias{anti_join.survey_collection}
\title{Domain-aware semi- and anti-join for survey designs}
\usage{
\method{semi_join}{survey_collection}(x, y, ..., .if_missing_var = NULL)

\method{anti_join}{survey_collection}(x, y, ..., .if_missing_var = NULL)

semi_join(x, y, by = NULL, copy = FALSE, ...)

anti_join(x, y, by = NULL, copy = FALSE, ...)
}
\arguments{
\item{x}{A \code{\link[surveycore:survey_base]{survey_base}} object.}

\item{y}{A plain data frame. Must not be a survey object.}

\item{...}{Additional arguments forwarded to the underlying dplyr function.}

\item{.if_missing_var}{Per-call override of \code{collection@if_missing_var}.
One of \code{"error"} or \code{"skip"}, or \code{NULL} (the default) to inherit the
collection's stored value. See
\code{\link[surveycore:set_collection_if_missing_var]{surveycore::set_collection_if_missing_var()}}.}

\item{by}{A character vector of column names or a \code{\link[dplyr:join_by]{dplyr::join_by()}}
specification. \code{NULL} uses all common column names.}

\item{copy}{Forwarded to the underlying dplyr function.}
}
\value{
A survey design object of the same type as \code{x} with the domain column
(\code{..surveycore_domain..}) updated. Row count unchanged. No new columns added.
}
\description{
\code{semi_join()} marks rows as in-domain when they have a match in \code{y}.
\code{anti_join()} marks rows as in-domain when they do NOT have a match in \code{y}.
Neither function removes rows or adds new columns — they are implemented as
domain operations, exactly like \code{\link[=filter]{filter()}}.
}
\details{
\subsection{Domain awareness}{

Unlike standard \code{dplyr::semi_join()} and \code{dplyr::anti_join()}, these
implementations never physically remove rows. Instead, unmatched (or matched,
for \code{anti_join}) rows are marked \code{FALSE} in the \code{..surveycore_domain..}
column of \verb{@data}, exactly as \code{\link[=filter]{filter()}} does. This preserves variance
estimation validity.
}

\subsection{Chaining}{

Multiple calls accumulate via AND: a row must satisfy every condition from
every \code{filter()}, \code{semi_join()}, and \code{anti_join()} call to remain in-domain.
}

\subsection{Duplicate keys in y}{

Duplicate keys in \code{y} collapse to a single \code{TRUE} (for \code{semi_join}) or a
single \code{FALSE} (for \code{anti_join}) per survey row. Row expansion is not
possible with these functions.
}

\subsection{@variables$domain sentinel}{

A typed S3 sentinel of class \code{"surveytidy_join_domain"} is appended to
\verb{@variables$domain}. Phase 1 consumers can use
\code{inherits(entry, "surveytidy_join_domain")} to distinguish join sentinels
from quosures.
}
}
\section{Survey collections}{

When called on a \code{\link[surveycore:survey_collection]{surveycore::survey_collection}}, \code{semi_join()} errors
unconditionally with class
\code{surveytidy_error_collection_verb_unsupported}. The semantics for joining
a plain data frame onto a multi-survey container are still being designed.
Apply the join inside a per-survey pipeline before constructing the
collection.


When called on a \code{\link[surveycore:survey_collection]{surveycore::survey_collection}}, \code{anti_join()} errors
unconditionally with class
\code{surveytidy_error_collection_verb_unsupported}. The semantics for joining
a plain data frame onto a multi-survey container are still being designed.
Apply the join inside a per-survey pipeline before constructing the
collection.
}

\examples{
# create a small survey object
df <- data.frame(
  psu = paste0("psu_", 1:5),
  strata = "s1",
  fpc = 100,
  wt = 1,
  y1 = 1:5
)
d <- surveycore::as_survey(
  df,
  ids = psu,
  weights = wt,
  strata = strata,
  fpc = fpc,
  nest = TRUE
)
keepers <- data.frame(y1 = c(1, 3, 5))

# semi_join: rows matching keepers stay in-domain
semi_join(d, keepers, by = "y1")

# anti_join: rows matching keepers are marked out-of-domain
anti_join(d, keepers, by = "y1")

}
\seealso{
Other joins: 
\code{\link{bind_cols}()},
\code{\link{bind_rows}()},
\code{\link{inner_join}},
\code{\link{left_join}},
\code{\link{right_join}}
}
\concept{joins}
