% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{make_factor}
\alias{make_factor}
\title{Convert a vector to a factor using value labels}
\usage{
make_factor(
  x,
  ordered = FALSE,
  drop_levels = TRUE,
  force = FALSE,
  na.rm = FALSE,
  .label = NULL,
  .description = NULL
)
}
\arguments{
\item{x}{Vector to convert. Must be a labelled numeric, plain numeric with
a \code{"labels"} attribute, R factor, or character vector.}

\item{ordered}{\code{logical(1)}. If \code{TRUE}, returns an ordered factor.}

\item{drop_levels}{\code{logical(1)}. If \code{TRUE} (the default), removes levels
with no observed values in \code{x}.}

\item{force}{\code{logical(1)}. If \code{TRUE}, coerce a numeric \code{x} without value
labels via \code{as.factor()}, issuing a
\code{surveytidy_warning_make_factor_forced} warning. If \code{FALSE} (the
default), error instead.}

\item{na.rm}{\code{logical(1)}. If \code{TRUE}, values in \code{attr(x, "na_values")} and
\code{attr(x, "na_range")} are converted to \code{NA} before building factor
levels, so they do not produce factor levels. Ignored for factor and
character input.}

\item{.label}{\code{character(1)} or \code{NULL}. Variable label override. If \code{NULL},
inherits from \code{attr(x, "label")}; if that is also \code{NULL}, falls back to
the column name.}

\item{.description}{\code{character(1)} or \code{NULL}. Transformation description
stored in \code{surveytidy_recode}.}
}
\value{
An R factor (ordered if \code{ordered = TRUE}).
}
\description{
\code{make_factor()} converts a labelled numeric, factor, or character vector to
an R factor. For labelled numeric input (e.g., from
\pkg{haven} or with a \code{"labels"} attribute), factor levels are derived from
the value labels. For factor input, levels are preserved. For character
input, levels are set alphabetically.

When called inside \code{\link[=mutate]{mutate()}}, metadata is recorded in
\verb{@metadata@transformations[[col]]}.
}
\examples{
library(dplyr)
d <- surveycore::as_survey(
  data.frame(x = c(1, 2, 1, 2), wt = c(1, 1, 1, 1)),
  weights = wt
)
x <- c(1, 2, 1, 2)
attr(x, "labels") <- c("Yes" = 1, "No" = 2)
make_factor(x)

}
\seealso{
Other transformation: 
\code{\link{make_binary}()},
\code{\link{make_dicho}()},
\code{\link{make_flip}()},
\code{\link{make_rev}()},
\code{\link{row_means}()},
\code{\link{row_sums}()}
}
\concept{transformation}
