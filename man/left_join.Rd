% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joins.R, R/reexports.R
\name{left_join}
\alias{left_join}
\title{Add columns from a data frame to a survey design}
\usage{
left_join(
  x,
  y,
  by = NULL,
  copy = FALSE,
  suffix = c(".x", ".y"),
  ...,
  keep = NULL
)
}
\arguments{
\item{x}{A \code{\link[surveycore:survey_base]{survey_base}} object.}

\item{y}{A plain data frame with lookup columns. Must not be a survey
object. Must not have column names matching any design variable in \code{x}
(those are dropped with a warning).}

\item{by}{A character vector of column names or a \code{\link[dplyr:join_by]{dplyr::join_by()}}
specification. \code{NULL} uses all common column names.}

\item{copy}{Forwarded to \code{\link[dplyr:mutate-joins]{dplyr::left_join()}}.}

\item{suffix}{A character vector of length 2 appended to deduplicate column
names shared between \code{x} and \code{y}. Forwarded to \code{\link[dplyr:mutate-joins]{dplyr::left_join()}}.}

\item{...}{Additional arguments forwarded to \code{\link[dplyr:mutate-joins]{dplyr::left_join()}}.}

\item{keep}{Forwarded to \code{\link[dplyr:mutate-joins]{dplyr::left_join()}}.}
}
\value{
A survey design object of the same type as \code{x} with new columns from \code{y}
appended to \verb{@data}. \code{visible_vars} is updated if it was set.
}
\description{
\code{left_join()} adds columns from a plain data frame \code{y} to a survey design
object \code{x}, matching on keys defined by \code{by}. All rows of \code{x} are preserved
(left join semantics). Rows with no match in \code{y} receive \code{NA} for the new
columns.
}
\details{
\subsection{Design integrity}{

\code{y} must be a plain data frame, not a survey object. If \code{y} has column names
that match any design variable in \code{x} (weights, strata, PSU, FPC,
replicate weights, or the domain column), those columns are dropped from \code{y}
with a warning before joining. Join keys in \code{by} are excluded from this
check.
}

\subsection{Row count}{

\code{left_join()} errors if \code{y} has duplicate keys that would expand \code{x} beyond
its original row count. Duplicate respondent rows corrupt variance
estimation. Deduplicate \code{y} with \code{dplyr::distinct()} before joining.
}

\subsection{Metadata}{

New columns from \code{y} receive no variable labels in \verb{@metadata}. If a column
in \code{x@data} is suffix-renamed because \code{y} has a non-design column with the
same name, the corresponding \verb{@metadata@variable_labels} key is updated to
the new suffixed name.
}
}
\examples{
library(dplyr)
library(surveycore)
df <- data.frame(
  psu = paste0("psu_", 1:5),
  strata = "s1",
  fpc = 100,
  wt = 1,
  y1 = 1:5
)
d <- as_survey(df, ids = psu, weights = wt, strata = strata, fpc = fpc,
               nest = TRUE)
lookup <- data.frame(y1 = 1:5, label = letters[1:5])
left_join(d, lookup, by = "y1")

}
\seealso{
Other joins: 
\code{\link{bind_cols}()},
\code{\link{bind_rows}()},
\code{\link{inner_join}},
\code{\link{right_join}},
\code{\link{semi_join}}
}
\concept{joins}
