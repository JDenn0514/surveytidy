% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{filter_out.survey_base}
\alias{filter_out.survey_base}
\title{Exclude rows from a survey domain}
\usage{
\method{filter_out}{survey_base}(.data, ..., .by = NULL, .preserve = FALSE)
}
\arguments{
\item{.data}{A \code{survey_taylor}, \code{survey_replicate}, or \code{survey_twophase}
object created by \code{\link[surveycore:as_survey]{surveycore::as_survey()}}.}

\item{...}{<\code{\link[rlang:args_data_masking]{data-masking}}> Logical conditions
evaluated against \verb{@data}. Rows where \strong{all} conditions are \code{TRUE} are
marked as out-of-domain. \code{NA} results are treated as \code{FALSE}
(the row stays in-domain).}

\item{.by}{Not supported for survey objects. Use \code{\link[dplyr:group_by]{dplyr::group_by()}} instead.}

\item{.preserve}{Ignored (included for compatibility with the dplyr
generic signature).}
}
\value{
The survey object with an updated \code{..surveycore_domain..} column in
\verb{@data}. Row count is \strong{unchanged}.
}
\description{
The complement of \code{\link[dplyr:filter]{dplyr::filter()}}. \code{filter_out()} marks rows \strong{matching} the
condition as out-of-domain while leaving all other rows in-domain. Like
\code{\link[dplyr:filter]{dplyr::filter()}}, it \strong{never removes rows} from the survey object.

\code{filter_out(.data, cond)} is equivalent to \code{filter(.data, !cond)} but
reads more naturally when the intent is exclusion.

Chained calls accumulate via AND: rows must satisfy all prior in-domain
conditions and none of the exclusion conditions to remain in-domain.
}
\examples{
library(dplyr)
df <- data.frame(y = rnorm(100), x = runif(100),
                 wt = runif(100, 1, 5), g = sample(c("A","B"), 100, TRUE))
d <- surveycore::as_survey(df, weights = wt)

# Exclude negative y values
d_out <- filter_out(d, y < 0)

# Equivalent to negating the condition in filter()
d_inv <- filter(d, !(y < 0))
identical(d_out@data[[surveycore::SURVEYCORE_DOMAIN_COL]],
          d_inv@data[[surveycore::SURVEYCORE_DOMAIN_COL]])

# Chain with filter() — only x > 0.5 rows that are NOT in group B
d_chain <- filter(d, x > 0.5) |> filter_out(g == "B")

}
\seealso{
\code{\link[dplyr:filter]{dplyr::filter()}} for including rows in the domain

Other filtering: 
\code{\link{filter.survey_base}()}
}
\concept{filtering}
