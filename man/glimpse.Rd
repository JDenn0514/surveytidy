% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collection-pull-glimpse.R, R/reexports.R,
%   R/select.R
\name{glimpse.survey_collection}
\alias{glimpse.survey_collection}
\alias{glimpse}
\alias{glimpse.survey_base}
\title{Get a glimpse of a survey design object}
\usage{
\method{glimpse}{survey_collection}(x, width = NULL, ..., .by_survey = FALSE)

glimpse(x, width = NULL, ...)

\method{glimpse}{survey_base}(x, width = NULL, ...)
}
\arguments{
\item{x}{A \code{\link[surveycore:survey_base]{survey_base}} object.}

\item{width}{Width of the output. Defaults to the console width.}

\item{...}{Passed to \code{dplyr::glimpse()}.}

\item{.by_survey}{If \code{TRUE}, render a separate labelled glimpse block per
member (\verb{▸ <member_name>}). Default \code{FALSE} renders a single bound tibble
with the source survey id prepended as \code{coll@id} (default \code{.survey}).}
}
\value{
\code{x} invisibly.

\code{x} invisibly.
}
\description{
Print a transposed summary of the survey object's columns — column names
run down the left, data types and values run across. Respects \code{\link[=select]{select()}}:
if columns have been selected, only those columns are shown; design
variables are hidden from the display.
}
\section{Survey collections}{

Default mode binds every member's \verb{@data} into a single tibble (via
\code{\link[dplyr:bind_rows]{dplyr::bind_rows()}} with \code{.id = coll@id}) and glimpses the result. If any
member's \verb{@data} already contains a column named \code{coll@id}, the
surveytidy_error_collection_glimpse_id_collision error is raised BEFORE
binding — symmetric with surveycore's
\code{surveycore_error_collection_id_collision} for the construction-time case.
Resolve by renaming the colliding column or setting a different \code{coll@id}
via \code{\link[surveycore:set_collection_id]{surveycore::set_collection_id()}}.

Internal column rename: when the member \verb{@data} contains
\link[surveycore:SURVEYCORE_DOMAIN_COL]{surveycore::SURVEYCORE_DOMAIN_COL} (\code{..surveycore_domain..}), the column
is renamed to \code{.in_domain} for the rendered output. Per-member \verb{@data} is
untouched.

Type-coercion footer: when \code{bind_rows()} coerces conflicting types across
members (e.g., \verb{<chr>} vs \verb{<dbl>}), a footer enumerates the affected
columns. Truncates after 5 columns; line width capped at 80 characters.
No opt-out — the footer renders only when conflicts exist.
}

\examples{
library(surveytidy)
library(surveycore)

# create a survey design from the pew_npors_2025 example dataset
d <- as_survey(pew_npors_2025, weights = weight, strata = stratum)

# glimpse all columns
glimpse(d)

# after select(), shows only the selected columns
d |>
  select(gender, agecat, partysum) |>
  glimpse()

}
\seealso{
\code{\link[=select]{select()}} to control which columns are visible

Other selecting: 
\code{\link{pull.survey_collection}()},
\code{\link{relocate}},
\code{\link{select}}
}
\concept{selecting}
