% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{make_rev}
\alias{make_rev}
\title{Reverse the numeric values of a scale variable}
\usage{
make_rev(x, .label = NULL, .description = NULL)
}
\arguments{
\item{x}{A numeric vector. \code{typeof(x)} must be \code{"double"} or \code{"integer"}.}

\item{.label}{\code{character(1)} or \code{NULL}. Variable label override. If \code{NULL},
inherits from \code{attr(x, "label")}; if that is also \code{NULL}, falls back to
the column name.}

\item{.description}{\code{character(1)} or \code{NULL}. Transformation description.}
}
\value{
A numeric vector (same \code{typeof()} as \code{x}) with reversed values.
}
\description{
\code{make_rev()} reverses the direction of a numeric scale variable using the
formula \code{min(x) + max(x) - x}. This preserves the scale range: a 1-4 scale
reversed stays a 1-4 scale; a 2-5 scale reversed stays a 2-5 scale.

Value labels are remapped: each label's numeric value becomes \code{min + max - old_value}, so the label string stays tied to its original concept at its
new position.

When called inside \code{\link[=mutate]{mutate()}}, metadata is recorded in
\verb{@metadata@transformations[[col]]}.
}
\examples{
# reverse a 1–4 numeric scale: 1↔4 and 2↔3
x <- c(1, 2, 3, 4)
make_rev(x)

}
\seealso{
Other transformation: 
\code{\link{make_binary}()},
\code{\link{make_dicho}()},
\code{\link{make_factor}()},
\code{\link{make_flip}()},
\code{\link{row_means}()},
\code{\link{row_sums}()}
}
\concept{transformation}
