% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replace-values.R
\name{replace_values}
\alias{replace_values}
\title{Partially update values using an explicit mapping}
\usage{
replace_values(
  x,
  ...,
  from = NULL,
  to = NULL,
  .label = NULL,
  .value_labels = NULL,
  .description = NULL
)
}
\arguments{
\item{x}{Vector to partially update.}

\item{...}{These dots are for future extensions and must be empty.}

\item{from}{Vector of old values to replace. Must be the same type as \code{x}.}

\item{to}{Vector of new values corresponding to \code{from}. Must be the same
length as \code{from}.}

\item{.label}{\code{character(1)} or \code{NULL}. Variable label stored in
\verb{@metadata@variable_labels} after \code{\link[=mutate]{mutate()}}. Overrides the label
inherited from \code{x}.}

\item{.value_labels}{Named vector or \code{NULL}. Value labels stored in
\verb{@metadata@value_labels}. Names are the label strings; values are the
data values. Merged with any existing labels inherited from \code{x}; entries
in \code{.value_labels} take precedence over inherited entries with the same
name.}

\item{.description}{\code{character(1)} or \code{NULL}. Plain-language description
of how the variable was created. Stored in
\verb{@metadata@transformations[[col]]$description} after \code{\link[=mutate]{mutate()}}.}
}
\value{
An updated version of \code{x} with the same type and size. If \code{x}
carries labels or any surveytidy args are supplied, returns a
\code{haven_labelled} vector; otherwise returns the same type as \code{x}.
}
\description{
\code{replace_values()} replaces each value of \code{x} found in \code{from} with the
corresponding value from \code{to}. Values not found in \code{from} retain their
original value unchanged.

Use \code{replace_values()} when updating only specific values of an existing
variable. When remapping the full range of values in \code{x}, \code{\link[=recode_values]{recode_values()}}
is a better choice.

\code{replace_values()} automatically inherits value labels and the variable
label from \code{x}. Supply \code{.label} or \code{.value_labels} to override the inherited
values.

When any of \code{.label}, \code{.value_labels}, or \code{.description} are supplied, or
when \code{x} carries existing labels, output label metadata is written to
\verb{@metadata} after \code{\link[=mutate]{mutate()}}. When none apply, the output is the same type
as \code{x}.
}
\examples{
library(surveycore)
library(surveytidy)

# create the survey design
ns_wave1_svy <- as_survey_nonprob(ns_wave1, weights = weight)

# basic replace_values — replace pid3 == 4 ("Something else") with 3
new <- ns_wave1_svy |>
  mutate(pid3_clean = replace_values(pid3, from = 4, to = 3)) |>
  select(pid3, pid3_clean)

new

# value labels from pid3 carry over to pid3_clean automatically
new@metadata@value_labels

# override the inherited variable label via .label
new <- ns_wave1_svy |>
  mutate(
    pid3_clean = replace_values(
      pid3,
      from = 4,
      to = 3,
      .label = "Party ID (3 categories)"
    )
  ) |>
  select(pid3, pid3_clean)

new@metadata@variable_labels

# provide updated value labels that reflect the recoded categories
new <- ns_wave1_svy |>
  mutate(
    pid3_clean = replace_values(
      pid3,
      from = 4,
      to = 3,
      .label = "Party ID (3 categories)",
      .value_labels = c(
        "Democrat" = 1,
        "Republican" = 2,
        "Independent/Other" = 3
      )
    )
  ) |>
  select(pid3, pid3_clean)

new@metadata@value_labels

# attach a plain-language description of the transformation
new <- ns_wave1_svy |>
  mutate(
    pid3_clean = replace_values(
      pid3,
      from = 4,
      to = 3,
      .label = "Party ID (3 categories)",
      .description = paste(
        "'Something else' (pid3 == 4) replaced with",
        "value 3 (Independent)."
      )
    )
  ) |>
  select(pid3, pid3_clean)

new@metadata@transformations

}
\seealso{
\itemize{
\item \code{dplyr::replace_values()} for the base implementation.
\item \code{\link[=recode_values]{recode_values()}} for full value remapping with explicit \code{from}/\code{to}
vectors; does not inherit labels from \code{x} automatically.
\item \code{\link[=replace_when]{replace_when()}} for condition-based partial replacement.
\item \code{\link[=na_if]{na_if()}} to replace specific values with \code{NA}.
}

Other recoding: 
\code{\link{case_when}()},
\code{\link{if_else}()},
\code{\link{na_if}()},
\code{\link{recode_values}()},
\code{\link{replace_when}()}
}
\concept{recoding}
