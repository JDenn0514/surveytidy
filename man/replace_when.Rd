% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replace-when.R
\name{replace_when}
\alias{replace_when}
\title{Partially update a vector using conditional formulas}
\usage{
replace_when(x, ..., .label = NULL, .value_labels = NULL, .description = NULL)
}
\arguments{
\item{x}{A vector to partially update.}

\item{...}{<\code{\link[rlang:dyn-dots]{dynamic-dots}}> A sequence of two-sided
formulas (\code{condition ~ value}). The left-hand side must be a logical
vector the same size as \code{x}. The right-hand side provides the replacement
value, cast to the type of \code{x}. Cases are evaluated sequentially; the
first matching case is used. \code{NULL} inputs are ignored.}

\item{.label}{\code{character(1)} or \code{NULL}. Variable label stored in
\verb{@metadata@variable_labels} after \code{\link[=mutate]{mutate()}}. Overrides the label
inherited from \code{x}.}

\item{.value_labels}{Named vector or \code{NULL}. Value labels stored in
\verb{@metadata@value_labels}. Names are the label strings; values are the
data values. Merged with any existing labels inherited from \code{x}; entries
in \code{.value_labels} take precedence over inherited entries with the same
name.}

\item{.description}{\code{character(1)} or \code{NULL}. Plain-language description
of how the variable was created. Stored in
\verb{@metadata@transformations[[col]]$description} after \code{\link[=mutate]{mutate()}}.}
}
\value{
An updated version of \code{x} with the same type and size. If \code{x}
carries labels or any surveytidy args are supplied, returns a
\code{haven_labelled} vector; otherwise returns the same type as \code{x}.
}
\description{
\code{replace_when()} is a survey-aware version of \code{\link[dplyr:case-and-replace-when]{dplyr::replace_when()}} that
evaluates each formula case sequentially and replaces matching elements of
\code{x} with the corresponding RHS value. Elements where no case matches retain
their original value from \code{x}.

Use \code{replace_when()} when partially updating an existing vector. When
creating an entirely new vector from conditions, \code{\link[=case_when]{case_when()}} is a better
choice.

\code{replace_when()} automatically inherits value labels and the variable label
from \code{x}. Supply \code{.label} or \code{.value_labels} to override the inherited
values.

When any of \code{.label}, \code{.value_labels}, or \code{.description} are supplied, or
when \code{x} carries existing labels, output label metadata is written to
\verb{@metadata} after \code{\link[=mutate]{mutate()}}. When none apply, the output is identical to
\code{\link[dplyr:case-and-replace-when]{dplyr::replace_when()}}.
}
\examples{

library(surveycore)
library(surveytidy)
ns_wave1_svy <- as_survey_calibrated(ns_wave1, weights = weight)

# ---------------------------------------------------------------------
# Basic replace_when — identical to dplyr::replace_when() -------------
# ---------------------------------------------------------------------

# Replace "Something else" (pid3 == 4) with 3 (Independent).
# Only matching rows change; all others keep their original value.
new <- ns_wave1_svy |>
  mutate(pid3_clean = replace_when(pid3, pid3 == 4 ~ 3)) |>
  select(pid3, pid3_clean)

new

# Value labels from pid3 carry over to pid3_clean automatically
new@metadata@value_labels


# ---------------------------------------------------------------------
# Set metadata --------------------------------------------------------
# ---------------------------------------------------------------------

# ---- Variable label ----

# Override the variable label inherited from pid3
new <- ns_wave1_svy |>
  mutate(
    pid3_clean = replace_when(
      pid3,
      pid3 == 4 ~ 3,
      .label = "Party ID (3 categories)"
    )
  ) |>
  select(pid3, pid3_clean)

new@metadata@variable_labels


# ---- Value labels ----

# Provide updated value labels that reflect the collapsed categories
new <- ns_wave1_svy |>
  mutate(
    pid3_clean = replace_when(
      pid3,
      pid3 == 4 ~ 3,
      .label = "Party ID (3 categories)",
      .value_labels = c(
        "Democrat" = 1,
        "Republican" = 2,
        "Independent/Other" = 3
      )
    )
  ) |>
  select(pid3, pid3_clean)

new@metadata@value_labels


# ---- Transformation ----

new <- ns_wave1_svy |>
  mutate(
    pid3_clean = replace_when(
      pid3,
      pid3 == 4 ~ 3,
      .label = "Party ID (3 categories)",
      .description = "Recoded pid3: 'Something else' (4) merged into Independent (3)."
    )
  ) |>
  select(pid3, pid3_clean)

new@metadata@transformations

}
\seealso{
\itemize{
\item \code{\link[dplyr:case-and-replace-when]{dplyr::replace_when()}} for the base implementation.
\item \code{\link[=case_when]{case_when()}} to create an entirely new vector from conditions.
\item \code{\link[=replace_values]{replace_values()}} for in-place replacement using an explicit \code{from}/\code{to}
mapping rather than conditions.
}

Other recoding: 
\code{\link{case_when}()},
\code{\link{if_else}()},
\code{\link{na_if}()},
\code{\link{recode_values}()},
\code{\link{replace_values}()}
}
\concept{recoding}
