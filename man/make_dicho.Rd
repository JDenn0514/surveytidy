% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{make_dicho}
\alias{make_dicho}
\title{Collapse a multi-level factor to two levels}
\usage{
make_dicho(
  x,
  flip_levels = FALSE,
  .exclude = NULL,
  .label = NULL,
  .description = NULL
)
}
\arguments{
\item{x}{Vector. Same types as \code{\link[=make_factor]{make_factor()}}.}

\item{flip_levels}{\code{logical(1)}. If \code{TRUE}, reverse the order of the two
output levels.}

\item{.exclude}{\code{character} or \code{NULL}. Level name(s) to set to \code{NA} before
collapsing. Intended for middle categories and "don't know"/"refused".}

\item{.label}{\code{character(1)} or \code{NULL}. Variable label override. Falls back
to \code{attr(x, "label")} then the column name.}

\item{.description}{\code{character(1)} or \code{NULL}. Transformation description.}
}
\value{
A 2-level R factor.
}
\description{
\code{make_dicho()} converts a variable to a two-level factor by stripping the
first qualifier word from each level label and grouping the resulting stems.
For example, a 4-level Likert scale with labels
\code{c("Strongly agree", "Agree", "Disagree", "Strongly disagree")} collapses
to \code{c("Agree", "Disagree")} by removing the qualifier "Strongly".

When called inside \code{\link[=mutate]{mutate()}}, metadata is recorded in
\verb{@metadata@transformations[[col]]}.
}
\examples{
library(dplyr)
x <- factor(
  c("Always agree", "Sometimes agree", "Sometimes disagree", "Always disagree"),
  levels = c("Always agree", "Sometimes agree", "Sometimes disagree",
             "Always disagree")
)
make_dicho(x)

}
\seealso{
Other transformation: 
\code{\link{make_binary}()},
\code{\link{make_factor}()},
\code{\link{make_flip}()},
\code{\link{make_rev}()}
}
\concept{transformation}
